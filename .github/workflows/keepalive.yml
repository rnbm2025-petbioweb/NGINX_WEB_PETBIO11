name: ♻️ Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: 🌍 Ping Render web service
        run: curl -sSf https://petbio.siac2025.com > /dev/null

