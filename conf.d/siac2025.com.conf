# ======================================
# 🌐 SITIO PRINCIPAL siac2025.com
# ======================================

# ===============================================
# 🧭 Redirección HTTP a HTTPS
# Render ya aplica HTTPS automáticamente, pero
# mantenemos esta redirección por consistencia.
# ===============================================
server {
    listen 80;
    server_name siac2025.com www.siac2025.com;

    # Render hace proxy HTTPS, así que esto funcionará correctamente
    return 301 https://$host$request_uri;
}

# ===============================================
# 🧩 Bloque principal del sitio siac2025.com
# Se ajusta para Render: solo listen 80 (no 443)
# Render maneja SSL externamente.
# ===============================================
server {
    listen 80;  # ✅ Antes: 443 (Render no necesita eso)
    server_name siac2025.com www.siac2025.com;

    root /var/www/siac2025.com;
    index index.php index.html;

    # 🔐 Render maneja el SSL externamente
    include /etc/nginx/security-headers.conf;

    # ===============================================
    # 📁 Enrutamiento principal
    # Si el archivo existe, lo sirve, si no, 404
    # ===============================================
    location / {
        try_files $uri $uri/ =404;
    }

    # ===============================================
    # 🚫 Archivos sensibles
    # Protege configuraciones, logs y backups
    # ===============================================
    location ~* \.(?:conf|php|sh|sql|bak|env|yml|ini|log)$ {
        deny all;
        return 403;
    }

    # ===============================================
    # 📄 Archivo especial (ads.txt)
    # Sirve un texto plano desde el root
    # ===============================================
    location = /ads.txt {
        default_type text/plain;
        root /var/www/siac2025.com;
    }

    # ===============================================
    # 🖼️ Archivos estáticos (cacheados por 30 días)
    # ===============================================
    location ~* \.(?:jpg|jpeg|png|gif|ico|webp|svg|css|js|woff2?|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
        access_log off;
    }

    access_log /var/log/nginx/siac2025-access.log;
    error_log  /var/log/nginx/siac2025-error.log;
}
